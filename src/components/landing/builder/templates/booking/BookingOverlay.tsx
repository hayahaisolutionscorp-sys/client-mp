"use client";

import type { BookingTemplateProps } from "../../types";
import LandingBookingSection from "../../LandingBookingSection";

export default function BookingOverlay({ theme }: BookingTemplateProps) {
  return <LandingBookingSection variant="default" />;
}
