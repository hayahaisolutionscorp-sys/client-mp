/**
 * Last-resort values for inline styles when theme hooks have no explicit hex yet.
 * Must match tokens set on :root in app/layout.tsx and ThemeProvider.
 */
export const FALLBACK_CSS_PRIMARY = "hsl(var(--primary))";
export const FALLBACK_CSS_SECONDARY = "hsl(var(--secondary))";
export const FALLBACK_CSS_ACCENT = "hsl(var(--accent))";
export const FALLBACK_CSS_SURFACE = "var(--surface)";
export const FALLBACK_CSS_SURFACE_ALT = "var(--surface-alt)";
